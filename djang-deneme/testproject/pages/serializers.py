# basicly help to convert the complex types or model instances into native python data types that can be easily rendered into json or xml or other content types. 
# they also help into deserialization which is nothing but converting the past data back to complex types.

#   elimle yazdım ing. olduğuna bakma emek var emek :( (be cool!)

#from rest_framework import serializers
#from pages.models import Departments,Employees

'''
class DeparmentsSerializer(serializers.ModelSerializer):
    class Meta:
        model=Departments
        fields =('DepartmentId','DepartmentName')
        
class EmployeeSerializer(serializers.ModelSerializer):
    class Meta:
        model=Employees
        fields =('EmployeeId','EmployeeName','Department','DateOfJoining','PhotoFileName')
        '''